# Every comment removed from the rule packages

Generated by `tools/comments` from `/home/skag/repos/personal/tsgolint-rules/.lintcn`. The comments below no longer
exist in the source; this file is the record of them.

694 comment lines removed from 44 files (5777 lines of Go). 57 lines of
`// lintcn:` metadata were kept -- lintcn's discovery reads them by regex.

| Package | File | Groups | Comment lines | % of file |
|---|---|---:|---:|---:|
| `archkit` | `gate.go` | 2 | 9 | 41% |
| `archkit` | `gate_test.go` | 1 | 1 | 2% |
| `archkit` | `scope.go` | 9 | 34 | 23% |
| `archkit` | `scope_test.go` | 1 | 1 | 1% |
| `archkit` | `types.go` | 18 | 55 | 24% |
| `archkit` | `types_test.go` | 0 | 0 | 0% |
| `context_model_does_not_cross_the_boundary` | `context_model_does_not_cross_the_boundary.go` | 10 | 30 | 16% |
| `context_model_does_not_cross_the_boundary` | `context_model_does_not_cross_the_boundary_test.go` | 17 | 28 | 15% |
| `domain_function_returns_an_answer` | `domain_function_returns_an_answer.go` | 6 | 15 | 15% |
| `domain_function_returns_an_answer` | `domain_function_returns_an_answer_test.go` | 11 | 15 | 16% |
| `domain_probe_returns_void` | `domain_probe_returns_void.go` | 2 | 6 | 7% |
| `domain_probe_returns_void` | `domain_probe_returns_void_test.go` | 6 | 9 | 11% |
| `domain_signature_stays_in_the_domain` | `domain_signature_stays_in_the_domain.go` | 8 | 27 | 15% |
| `domain_signature_stays_in_the_domain` | `domain_signature_stays_in_the_domain_test.go` | 9 | 16 | 11% |
| `domain_state_is_deeply_readonly` | `domain_state_is_deeply_readonly.go` | 6 | 13 | 6% |
| `domain_state_is_deeply_readonly` | `domain_state_is_deeply_readonly_test.go` | 19 | 26 | 15% |
| `domain_type_is_declared_once` | `domain_type_is_declared_once.go` | 9 | 30 | 18% |
| `domain_type_is_declared_once` | `domain_type_is_declared_once_test.go` | 12 | 20 | 19% |
| `driving_port_command_is_modelled` | `driving_port_command_is_modelled.go` | 4 | 11 | 11% |
| `driving_port_command_is_modelled` | `driving_port_command_is_modelled_test.go` | 8 | 11 | 11% |
| `no_constructed_collaborators` | `no_constructed_collaborators.go` | 5 | 15 | 22% |
| `no_constructed_collaborators` | `no_constructed_collaborators_test.go` | 8 | 13 | 16% |
| `no_double_library_in_domain` | `no_double_library_in_domain_test_test.go` | 5 | 8 | 11% |
| `no_double_library_in_domain` | `rule.go` | 2 | 6 | 7% |
| `no_double_library_in_use_case` | `no_double_library_in_use_case_test_test.go` | 6 | 8 | 9% |
| `no_double_library_in_use_case` | `rule.go` | 2 | 6 | 7% |
| `no_outside_declaration_in_the_hexagon` | `no_outside_declaration_in_the_hexagon.go` | 3 | 9 | 8% |
| `no_outside_declaration_in_the_hexagon` | `no_outside_declaration_in_the_hexagon_test.go` | 13 | 20 | 11% |
| `no_page_request_in_journey` | `no_page_request_in_journey.go` | 2 | 6 | 11% |
| `no_page_request_in_journey` | `no_page_request_in_journey_test.go` | 5 | 7 | 9% |
| `no_provider_type_in_signature` | `no_provider_type_in_signature.go` | 9 | 37 | 13% |
| `no_provider_type_in_signature` | `no_provider_type_in_signature_test.go` | 33 | 43 | 12% |
| `port_behaviour_is_an_interface` | `port_behaviour_is_an_interface.go` | 5 | 17 | 14% |
| `port_behaviour_is_an_interface` | `port_behaviour_is_an_interface_test.go` | 13 | 21 | 22% |
| `published_contract_publishes_no_mutable_value` | `published_contract_publishes_no_mutable_value.go` | 8 | 20 | 14% |
| `published_contract_publishes_no_mutable_value` | `published_contract_publishes_no_mutable_value_test.go` | 12 | 16 | 16% |
| `read_port_returns_an_answer` | `read_port_returns_an_answer.go` | 4 | 11 | 11% |
| `read_port_returns_an_answer` | `read_port_returns_an_answer_test.go` | 8 | 11 | 11% |
| `stored_state_switch_has_a_throwing_default` | `stored_state_switch_has_a_throwing_default.go` | 6 | 18 | 14% |
| `stored_state_switch_has_a_throwing_default` | `stored_state_switch_has_a_throwing_default_test.go` | 10 | 17 | 9% |
| `use_case_result_is_discriminated` | `use_case_result_is_discriminated.go` | 8 | 20 | 9% |
| `use_case_result_is_discriminated` | `use_case_result_is_discriminated_test.go` | 26 | 35 | 12% |
| `use_case_throws_a_domain_error` | `use_case_throws_a_domain_error.go` | 4 | 13 | 15% |
| `use_case_throws_a_domain_error` | `use_case_throws_a_domain_error_test.go` | 10 | 17 | 16% |

---

## `.lintcn/archkit/gate.go`

**L1**
> Package archkit is the shared helper package for the lintcn rules in this
> repository. It replaces the old fork's internal packages `archscope`,
> `archtypes`, and `archrule`: `rule.Rule` has no Files field in lintcn's
> registration, so each rule gates its own listeners with Includes instead.

**L9**
> Gated wraps a rule's Run so the rule only analyzes files its tree names.
> 
> lintcn registers plain rule.Rule values with no per-rule file scope, so this
> is where the old `archrule.Rule{Files: ...}` scoping lives now: a rule whose
> file matches none of the patterns gets no listeners at all.


## `.lintcn/archkit/gate_test.go`

**L26**
> The default test file name matches no e2e tree, so the rule must not run.


## `.lintcn/archkit/scope.go`

**L1**
> File-path scoping: `.gitignore`-style glob matching and declaration-file
> classification, ported from the old fork's `internal/archscope`.

**L12**
> Includes reports whether fileName is judged by any of the patterns.
> 
> Patterns are `.gitignore`-style globs over path segments: `**` matches any
> run of segments including none, and `*`/`?`/`[…]` match within one segment.

**L44**
> `**` matches nothing at all, or one more segment and then itself.

**L63**
> IsOutsideDependency reports whether a declaration file belongs to a package
> dependency or to the TypeScript standard library rather than to this
> repository's own source.
> 
> This is the one cross-file fact a per-file syntactic rule cannot reach, and
> the whole reason several of these rules can exist at all.

**L73**
> IsPackageDependency reports whether a declaration file was installed rather
> than written here.

**L84**
> IsStandardLibrary reports whether a declaration file is one of TypeScript's
> bundled `lib.*.d.ts` files — where `Date`, `Map` and `Response` alike are
> declared, so a rule must decide deliberately which side of the line it wants.

**L92**
> Compile turns configured patterns into expressions, dropping any that do not
> compile: a misconfigured pattern identifies nothing rather than failing a run.

**L104**
> ContextOf returns the identity the first matching pattern captures in a
> `(?<name>…)` group, taken at the innermost place that pattern matches.
> 
> Innermost, because these file names are absolute and a pattern is free to
> match above the repository as well as inside it. A checkout under
> `~/packages/…`, or this repository's own fixtures under
> `packages/tsgolint/fixtures/`, made every context below answer that outer
> segment; they then compared equal and a real crossing went unreported.
> 
> Matches are found by restarting one byte past each match's start rather than
> past its end, so a pattern that consumes the separator between two contexts
> — as `(^|/)packages/(?<name>[^/]+)(/|$)` does — still sees the second one.

**L141**
> captureIndex is the position of the `name` group, or -1 when the pattern has
> none and so identifies nothing.


## `.lintcn/archkit/scope_test.go`

**L62**
> A repository file merely mentioning the word is ours.


## `.lintcn/archkit/types.go`

**L1**
> Checker questions the lintcn rules in this repository share.
> 
> All of them come down to the same move: resolve a written thing to a type,
> resolve that type to the file that declares it, and judge the file. That is
> the fact a syntactic rule cannot reach, and it is why these rules are in Go.
> 
> Helpers are added when the first rule needs them, ported from the old
> fork's `internal/archtypes`, not kept speculatively.

**L20**
> DeclaringFiles returns the files declaring a type's symbol. An anonymous
> type — an object literal type, a union — has no symbol and no declaration,
> and returns nothing rather than a guess.

**L28**
> The nil arms of this check and the sourceFile check below are exercised
> by the rule packages' rule_tester cases (an anonymous object-literal
> type has no symbol; a synthesized declaration has no source file), and
> so are the checker-dependent arms of the helpers below (a non-array
> type, an unawaited one; DeclaredUnder's nil symbol, reached by a
> call-expression base in the double-library tests). The TypeReferenceNames
> and declaredName walks run on every rule_tester signature. Gremlins
> cannot see that coverage cross-package, so the ten surviving NOT COVERED
> mutants in this file are recorded as covered there; DeclaredType's two
> nil arms are defensive and documented at the function.

**L44**
> DeclaringFilesOfSymbol returns the files declaring a symbol.

**L59**
> DeclaredUnder reports whether a written expression resolves to a symbol
> declared in a file containing any of the path fragments — how a property
> access is told apart from the library's double methods by where the value
> behind it is declared.

**L83**
> Constituents flattens a union into its members, and returns a non-union
> unchanged. A check that skips this passes `string | undefined`, which is the
> recorded way one of these rules stops enforcing anything.

**L96**
> ElementTypes returns the element types of an array or tuple, and nothing for
> any other type. `readonly string[]` is a reference type rather than a
> string-flagged one, so a rule reading only type flags finds no string in it.

**L106**
> Unwrapped resolves `Promise<T>` to `T`, and leaves a synchronous type alone.
> Every port in this repository is async, so a rule that judges a return type
> without this judges `Promise` instead.

**L119**
> CallSignatures returns a type's call signatures.

**L127**
> IsCallable reports whether a type is a function — behaviour, which is not
> state and not worth following for its mutability.

**L133**
> Members returns a type's properties paired with their own types, which is
> how both `{ save: (o) => void }` and `interface P { save(o): void }` are read
> as the same contract.

**L151**
> Member is one property of a declared contract.

**L157**
> IsVoidLike reports whether a type carries no answer — the shape of a write.

**L162**
> DeclaredType resolves an interface or type-alias declaration to the type it
> declares. The nil arms are defensive: the consumers call it on
> interface/type-alias declarations whose names always resolve.

**L177**
> TypeReferenceNames returns the written type names a type annotation
> mentions, qualified names reduced to their rightmost name. This is how a
> signature's vocabulary is read syntactically — before resolution decides
> whether each name is foreign.

**L200**
> declaredName reduces a type name, which may be qualified like
> `some.Namespace.Type`, to the rightmost identifier the resolver knows.

**L209**
> WrittenName is the rightmost name a written reference carries — how `new
> z.ZodError([])` and `new ZodError([])` are reported by the same name.

**L227**
> ReturnType returns a signature's return type.


## `.lintcn/archkit/types_test.go`

_No comments._

## `.lintcn/context_model_does_not_cross_the_boundary/context_model_does_not_cross_the_boundary.go`

**L1** _(kept in source)_
> lintcn:name context-model-does-not-cross-the-boundary
> lintcn:severity error
> lintcn:description Disallow an exported signature from naming a type declared inside another context's internals

**L19**
> A context's identity, captured by a group named `name` — the same decision
> `no-cross-context-internal-import` records: one context spelled two ways must
> capture the same text, or an intra-context reference reads as a crossing.
> 
> Spelled without the lookahead its JavaScript sibling uses. Go's regexp is
> RE2 and has no lookaround; only the capture is read, so a trailing group
> costs nothing.

**L28**
> A file another context may legitimately be reached through.
> A barrel is not a published contract: it is what a context ends up publishing
> by accident. dependency-cruiser forbids importing an index file at all, so it
> cannot also be the surface a second context is allowed to depend on.

**L34**
> Trees that belong to no context and are shared on purpose.

**L69**
> Inline literal: lintcn's discovery matches `Name: "..."` in the source to
> bind the CLI name to this rule, so a const would silently desynchronize
> from lintcn:name above.

**L79**
> A file no pattern identifies belongs to no context, and this rule has
> nothing to compare. Returning early is the shipped precedent, and the
> pages record that it makes a misconfigured pattern a green run.

**L87**
> crossing reports the first declaring file that puts a written type
> name inside another context's internals.

**L109**
> Declared *in* another context's published contract is the
> surface that context offered. Merely re-exported through one
> is not: the declaration still lives in its internals, which
> is the leak §2.3 names.

**L162**
> A published `interface` or `type` carries the model just as a
> function signature does, and is how a context most often leaks.

**L180**
> A `const` carries its `export` on the statement, two nodes up.


## `.lintcn/context_model_does_not_cross_the_boundary/context_model_does_not_cross_the_boundary_test.go`

**L13**
> Gifting's model, declared in its internals.

**L17**
> Its published contract re-exports the model rather than declaring one.
> That is the leak §2.3 names, and it is what the path rule cannot see.

**L29**
> A barrel at the context root. dependency-cruiser's no-barrel-files
> forbids importing one at all, so it cannot also be the surface this
> rule lets a second context depend on.

**L37**
> A checkout whose own path holds a `packages/` segment, which is what this
> repository's fixtures became when they moved to `packages/tsgolint/fixtures/`.

**L53**
> This context's own model.

**L62**
> A type the other context genuinely declares in its published
> contract is the surface it agreed to keep stable.

**L74**
> The shared kernel belongs to no context.

**L83**
> Not exported, so not part of this context's surface.

**L92**
> Its own context, reached by the other spelling of the same identity.

**L101**
> No pattern identifies this path as a context, so there is nothing to
> compare and the rule keeps its hands off.

**L111**
> The innermost `packages/<name>` is the context, so a file's own
> context is still its own when an outer directory is named that way.

**L122**
> One context's model named directly in another's signature.

**L132**
> The blind spot the path rule cannot close: the import is legal —
> `packages/gifting/public` is the front door — and the type it hands
> over is still declared in Gifting's internals.

**L146**
> A published type carries the model just as a signature does.

**L156**
> The return half.

**L169**
> The same crossing under an outer `packages/` segment. Read from the
> left, every context below it answers `monorepo`, they compare equal,
> and a real crossing goes unreported.

**L181**
> A root barrel is not a published contract.


## `.lintcn/domain_function_returns_an_answer/domain_function_returns_an_answer.go`

**L1** _(kept in source)_
> lintcn:name domain-function-returns-an-answer
> lintcn:severity error
> lintcn:description Disallow exported domain functions with no written return type whose inferred return is void or undefined on every path

**L25**
> finalReturn follows a curried chain to the type its last call answers with.
> `Promise` is deliberately not unwrapped: the domain is synchronous, and
> waiting is `no-async-in-domain`'s subject.

**L39**
> answerless is true only when every constituent carries no answer. A lookup
> returning `Occasion | undefined` answers "not found", and is not this rule's
> subject.

**L55**
> Inline literal: lintcn's discovery matches `Name: "..."` in the source to
> bind the CLI name to this rule, so a const would silently desynchronize
> from lintcn:name above.

**L64**
> A written return type is `no-void-return-in-domain`'s subject;
> this rule judges only what was left to inference.

**L87**
> A `const` carries its `export` on the statement, two nodes up.


## `.lintcn/domain_function_returns_an_answer/domain_function_returns_an_answer_test.go`

**L20**
> The next state, inferred rather than annotated — which is the case the
> syntactic rule cannot judge.

**L26**
> An explicit refusal or the next state: a union, and an answer either way.

**L34**
> A lookup that found nothing still answers.

**L39**
> The written `: void` is `no-void-return-in-domain`'s to report.

**L44**
> A curried transition answers at the end of the chain.

**L49**
> Not exported, so a local choice.

**L54**
> The gate: outside the domain tree (default file name `file.ts`) the
> rule reports nothing, however answerless the function is.

**L60**
> Inferred `void`: whatever it decided happened to something else, and
> the recorded hole in `no-void-return-in-domain`, which reads only what
> was written.

**L68**
> The decision landed in the caller's object, and nothing came back.

**L77**
> Inferred `undefined` on every path is no answer either.

**L83**
> Curried, and answerless at the end of the chain.


## `.lintcn/domain_probe_returns_void/domain_probe_returns_void.go`

**L1** _(kept in source)_
> lintcn:name domain-probe-returns-void
> lintcn:severity error
> lintcn:description Require Domain Probe methods to return void — a probe announces a fact fire-and-forget, not an answer

**L43**
> Inline literal: lintcn's discovery matches `Name: "..."` in the source to
> bind the CLI name to this rule, so a const would silently desynchronize
> from lintcn:name above.


## `.lintcn/domain_probe_returns_void/domain_probe_returns_void_test.go`

**L15**
> Fire-and-forget, spelled both ways the language offers.

**L25**
> A persistence port is not a probe, and its `Promise<void>` is an
> acknowledgement the caller is meant to wait for.

**L35**
> The gate: outside the ports tree (default file name `file.ts`) the
> rule reports nothing, however answering the probe is.

**L45**
> A probe that answers is a probe the caller can now depend on.

**L55**
> The recorded gap: the answer is behind an alias, which a syntactic rule
> reads as `void`.

**L67**
> Two answering members, reported once: the keyword is settled by the first.


## `.lintcn/domain_signature_stays_in_the_domain/domain_signature_stays_in_the_domain.go`

**L1** _(kept in source)_
> lintcn:name domain-signature-stays-in-the-domain
> lintcn:severity error
> lintcn:description Disallow domain signatures from naming types the domain does not own

**L17**
> The trees a domain signature may name. The shared kernel is here because
> `Money` is the domain's own vocabulary, published one level up.

**L51**
> where names the tree a foreign declaration came from, so the diagnostic says
> which boundary was crossed rather than only that one was.

**L60**
> reportable decides whether one declaration of a written type name puts that
> name outside the domain's own vocabulary.
> 
> A type alias declared by a package dependency is deliberately excused, on the
> precedent `no-provider-type-in-signature` already records: `z.infer<typeof
> Schema>` is declared in `node_modules` and describes a shape this repository
> owns, and flagging it would make every schema-derived domain type a
> violation. A class or interface from a dependency is nominal and is not
> excused. Everything this repository itself declares outside the allowed trees
> is reportable whichever way it is spelled — an application port is a port
> whether it is written `interface` or `type`.

**L78**
> The language itself is not foreign: `aggregate-root.md` holds
> `readonly pledgedAt: Date`, and without this the rule flags every domain
> type in the repository.

**L95**
> Inline literal: lintcn's discovery matches `Name: "..."` in the source to
> bind the CLI name to this rule, so a const would silently desynchronize
> from lintcn:name above.

**L103**
> foreign returns the first declaring file that puts a written type
> name outside the domain's vocabulary, and whether one was found.

**L169**
> A `const` carries its `export` on the statement, two nodes up.


## `.lintcn/domain_signature_stays_in_the_domain/domain_signature_stays_in_the_domain_test.go`

**L12**
> The three trees a domain signature may reach into, and the one it may not.

**L38**
> domain-service.md's Perfect Example signature: domain values in,
> a domain decision out.

**L52**
> The trap the ledger named: the standard library is not foreign.
> aggregate-root.md holds `readonly pledgedAt: Date`.

**L66**
> A package dependency's *type alias* is excused, on the precedent
> `no-provider-type-in-signature` records for `z.infer<typeof Schema>`.

**L77**
> Not exported, so not part of the model's published vocabulary.

**L86**
> The gate: outside the domain tree (default file name `file.ts`) the
> rule reports nothing, however foreign the signature is.

**L98**
> The claim: an application port is not a domain value, and no shipped
> rule sees it — `no-application-port-in-domain` judges declarations in
> the domain tree, not references to one declared elsewhere.

**L114**
> The return half, which `no-provider-type-in-signature` never reads:
> it visits parameters only.

**L127**
> A provider's nominal interface, reached through a generic argument.


## `.lintcn/domain_state_is_deeply_readonly/domain_state_is_deeply_readonly.go`

**L1** _(kept in source)_
> lintcn:name domain-state-is-deeply-readonly
> lintcn:severity error
> lintcn:description Disallow domain state whose members are writable deeper than the root guards

**L19**
> The containers whose element types are still domain state worth following.

**L57**
> owner is the interface, type alias or class declaration a node sits inside.

**L76**
> writtenCollection mirrors the syntactic rule's reading of a member's
> annotation: a `T[]`, or a reference named for a mutable container, is
> already `domain-state-is-readonly`'s to report.

**L123**
> Inline literal: lintcn's discovery matches `Name: "..."` in the source to
> bind the CLI name to this rule, so a const would silently desynchronize
> from lintcn:name above.

**L186**
> A member of another exported declaration is judged
> when that declaration is visited, not here.


## `.lintcn/domain_state_is_deeply_readonly/domain_state_is_deeply_readonly_test.go`

**L13**
> An ambient module, the shape many `@types` packages take: nothing inside
> carries an `export` keyword, so only the declaring file says it is a vendor's.

**L26**
> Readonly all the way down through the alias.

**L35**
> A member written mutable in the declaration itself is
> `domain-state-is-readonly`'s to report, not this rule's.

**L41**
> A member annotated with the `readonly` type operator is not a written
> mutable collection either, and the operator arm proves it.

**L47**
> A dependency's members are not this repository's state.

**L56**
> The standard library's own writable members are not the model's.

**L61**
> Behaviour is not state.

**L69**
> Recursion terminates on a self-referential type.

**L74**
> Another exported declaration is judged when it is visited, not
> through every type that names it.

**L83**
> Not exported, so not the model's published state.

**L91**
> The gate: outside the domain and shared-kernel trees (default file
> name `file.ts`) the rule reports nothing, however writable the state
> nested under the root is.

**L101**
> A member reached through a named type is writable.

**L110**
> An alias resolving to a mutable array.

**L119**
> The same alias, one level down.

**L129**
> A `Map` behind an alias, in a union with undefined.

**L138**
> The element type of a readonly collection is still state.

**L147**
> The shared kernel is judged too.

**L156**
> Reported once, at the declaration that owns the member: `Occasion`
> does not repeat what `FundingState`'s own visit already said.

**L167**
> Configured collection names.


## `.lintcn/domain_type_is_declared_once/domain_type_is_declared_once.go`

**L1** _(kept in source)_
> lintcn:name domain-type-is-declared-once
> lintcn:severity error
> lintcn:description Disallow declaring the same domain type twice in one context

**L20**
> A context's identity, captured by a group named `name` — the same spelling
> `context-model-does-not-cross-the-boundary` uses, and for the same reason.
> 
> The comparison is per context on purpose. `bounded-context.md` §2.1 is the
> language test: the same word meaning different things in two contexts is the
> boundary working, not a duplicate. Two files that no pattern identifies both
> answer "", compare equal, and are judged against each other — which is what a
> repository with no `packages/` layout needs.

**L31**
> Files is the tree the gate resolved for this rule, which is also the
> tree the index reads. This is the one rule that is told its own scope:
> every other rule judges the file it is given, while this one has to know
> which other files count as the same model.

**L63**
> index is the whole-program fact a per-file rule cannot have: which files
> export a top-level type under each name. Built once per program.

**L69** _(inline)_
> *compiler.Program -> *index

**L89**
> exportedTypeNames reads a file's top-level exported `interface` and `type`
> declarations. Nested and unexported declarations are not the model's
> published vocabulary and cannot be the canonical one.

**L112**
> contextOf keeps this rule's own comparison: a file no pattern identifies
> answers "", so two such files compare equal and are judged against each
> other — what a repository with no `packages/` layout needs.

**L121**
> Inline literal: lintcn's discovery matches `Name: "..."` in the source to
> bind the CLI name to this rule, so a const would silently desynchronize
> from lintcn:name above.

**L128**
> The gate reads the resolved tree, not the default: this is the one
> rule that is told its own scope, and the options' `files` moves both
> the gate and the index together.


## `.lintcn/domain_type_is_declared_once/domain_type_is_declared_once_test.go`

**L12**
> The second declaration is in another file, which is the whole point: no
> per-file rule can see it.

**L18**
> The same word in another context is the language test working, not a
> duplicate.

**L23**
> Outside the judged tree.

**L29**
> Two files in one context declaring one name, both outside the tree this
> rule declares.

**L40**
> Declared here and nowhere else in this context.

**L46**
> The language test: Gifting declares `Occasion` too, and the same
> word meaning something else in another context is the boundary
> working rather than a duplicate.

**L54**
> The same name outside the domain tree is not this rule's subject.

**L60**
> Unexported, so not the model's published vocabulary.

**L66**
> Two application files, one name, and the default tree indexes neither.

**L73**
> The canonical-type claim: two files in one context, two `Occasion`s,
> both compiling.

**L81**
> The same two files, once the tree the gate resolved says to index
> them. This is the one rule that is told its own tree, because it
> reads the tree rather than judging one file at a time.

**L91**
> Spelled as an interface, which is the same claim.


## `.lintcn/driving_port_command_is_modelled/driving_port_command_is_modelled.go`

**L1** _(kept in source)_
> lintcn:name driving-port-command-is-modelled
> lintcn:severity error
> lintcn:description Disallow driving port methods from taking bare primitives where a command belongs

**L19**
> The driving doc prefers a `For…` intention name. The rule does not require
> one — it uses it only to find the declarations it judges.

**L36**
> A primitive with nothing added. A branded `ContributorId` is an
> intersection, and a literal union is a modelled vocabulary; neither is bare,
> and no syntactic rule can tell them from `string`.

**L57**
> Inline literal: lintcn's discovery matches `Name: "..."` in the source to
> bind the CLI name to this rule, so a const would silently desynchronize
> from lintcn:name above.


## `.lintcn/driving_port_command_is_modelled/driving_port_command_is_modelled_test.go`

**L15**
> A modelled command, which is what the doc asks a driving port to take.

**L25**
> A branded value is an intersection, not a bare primitive — the whole
> reason this is a type-aware rule.

**L36**
> A literal union is a modelled vocabulary.

**L45**
> A driven port answering by id is not this rule's business.

**L54**
> The gate: outside the ports tree (default file name `file.ts`) the
> rule reports nothing, however bare the parameter is.

**L64**
> Loose primitives where a command belongs: the caller has to know the
> argument order, and nothing names what the operation needs.

**L75**
> An alias to a primitive is still a primitive.

**L86**
> A configured naming vocabulary reaches a port not called `For…`.


## `.lintcn/no_constructed_collaborators/no_constructed_collaborators.go`

**L1** _(kept in source)_
> lintcn:name no-constructed-collaborators
> lintcn:severity error
> lintcn:description Disallow the inside of the hexagon from constructing a provider a package dependency declares

**L28**
> Inline literal: lintcn's discovery matches `Name: "..."` in the source to
> bind the CLI name to this rule, so a const would silently desynchronize
> from lintcn:name above.

**L40**
> A construction over anything but a written name — a call, an
> element access — cannot be reported by name, and naming it
> is half of what this message says.

**L48**
> The constructed value's own type, not the instance's: a
> class's declaration is what says who wrote it.

**L53**
> Only an installed package. `new Date()`, `new Map()` and
> `new URL()` are declared in `lib.*.d.ts` and are values,
> not collaborators — banning them is the recorded reason
> the syntactic version of this rule was dropped.


## `.lintcn/no_constructed_collaborators/no_constructed_collaborators_test.go`

**L18**
> A package whose classes are reached through a namespace, so `new` is
> written against a property access rather than a bare name.

**L28**
> The values that made the syntactic version unshippable.

**L30**
> Standard-library values, which is the line `new` alone could not draw and
> the reason the syntactic version of this rule was refused.

**L34**
> A type this repository declares is not a provider.

**L42**
> The gate: outside the inside trees (default file name `file.ts`) the
> rule reports nothing, however provider-shaped the construction is.

**L49**
> A dependency constructed where it should have been injected.

**L56**
> An alias hides the name, never the declaration.

**L67**
> A class reached through its namespace. Asking a property access for
> its text as if it were an identifier took the whole run down, so
> every file after it went unjudged.


## `.lintcn/no_double_library_in_domain_test/no_double_library_in_domain_test_test.go`

**L25**
> A local `vi` is not the library, which only the declaration says.

**L33**
> A call-expression base has no symbol to resolve, which is the nil
> arm of archkit.DeclaredUnder; nothing is reported.

**L42**
> The gate: a file outside the domain test tree (default file name
> `file.ts`) reports nothing, however much double-library scaffolding
> it carries.

**L53**
> The doc's boundary: a domain test calls the model directly.

**L63**
> An alias hides the name, never the declaration.


## `.lintcn/no_double_library_in_domain_test/rule.go`

**L1** _(kept in source)_
> lintcn:name no-double-library-in-domain-test
> lintcn:severity warn
> lintcn:description Disallow test-double library scaffolding in domain tests

**L63**
> Inline literal: lintcn's discovery matches `Name: "..."` in the source to
> bind the CLI name to this rule, so a const would silently desynchronize
> from lintcn:name above.


## `.lintcn/no_double_library_in_use_case_test/no_double_library_in_use_case_test_test.go`

**L25**
> The doc's own answer: a stateful in-memory fake, hand-written.

**L37**
> A local `vi` is not the library, which only the declaration says.

**L45**
> The gate: a file outside the application test tree (default file name
> `file.ts`) reports nothing, however much double-library scaffolding it
> carries.

**L56**
> A mock standing in for the port the use-case test exists to exercise.

**L66**
> An alias hides the name, never the declaration.

**L76**
> Module mocking a port, in a `.tsx` use-case test.


## `.lintcn/no_double_library_in_use_case_test/rule.go`

**L1** _(kept in source)_
> lintcn:name no-double-library-in-use-case-test
> lintcn:severity warn
> lintcn:description Disallow test-double library scaffolding in use-case tests

**L63**
> Inline literal: lintcn's discovery matches `Name: "..."` in the source to
> bind the CLI name to this rule, so a const would silently desynchronize
> from lintcn:name above.


## `.lintcn/no_outside_declaration_in_the_hexagon/no_outside_declaration_in_the_hexagon.go`

**L1** _(kept in source)_
> lintcn:name no-outside-declaration-in-the-hexagon
> lintcn:severity error
> lintcn:description Disallow the inside of the hexagon from importing symbols declared in adapters, apps or composition

**L49**
> Inline literal: lintcn's discovery matches `Name: "..."` in the source to
> bind the CLI name to this rule, so a const would silently desynchronize
> from lintcn:name above.

**L57**
> outsideFile names the first declaring file of a symbol that sits in an
> outside tree. A package dependency never counts, whatever its path
> happens to contain.


## `.lintcn/no_outside_declaration_in_the_hexagon/no_outside_declaration_in_the_hexagon_test.go`

**L42**
> A port beside it and a schema library: both allowed, and the reason the
> rule resolves the symbol rather than banning package imports.

**L53**
> `adapters` in a package's own subpath is not this repository's adapter
> tree, which a path rule reading the specifier could not tell.

**L63**
> The barrel re-exports from inside as well as outside; this import
> resolves to the inside half.

**L73**
> The domain importing its own neighbour.

**L82**
> Configured scope replaces the default, so the adapter tree stops being
> outside and this import stops being anyone's business.

**L93**
> The gate: outside the hexagon trees (default file name `file.ts`) the
> rule reports nothing, however far outside the import reaches.

**L103**
> A use case reaching straight into the adapter tree.

**L113**
> The blind spot the path rules cannot close: the specifier is a legal
> neighbour, and the symbol behind it was declared in an adapter.

**L124**
> The same, through a workspace alias that only the resolver can follow.

**L135**
> A type-only import is still the domain knowing a stored row's shape.

**L145**
> A namespace import hides the name, never the declaration.

**L155**
> A side-effect import binds nothing, so there is no symbol to resolve —
> the module itself is what crossed the line.

**L165**
> A re-export hands the outside on without ever binding it either.


## `.lintcn/no_page_request_in_journey/no_page_request_in_journey.go`

**L1** _(kept in source)_
> lintcn:name no-page-request-in-journey
> lintcn:severity warn
> lintcn:description Disallow page.request in e2e journeys — it bypasses the browser the journey exists to prove

**L38**
> Inline literal: lintcn's discovery matches `Name: "..."` in the source to
> bind the CLI name to this rule, so a const would silently desynchronize
> from lintcn:name above.


## `.lintcn/no_page_request_in_journey/no_page_request_in_journey_test.go`

**L25**
> A journey driving the browser, which is the whole point of the file.

**L35**
> A local lookalike named `page`: same syntax, not Playwright's type, and
> the reason this rule resolves the declaration instead of matching text.

**L44**
> The gate: outside the e2e tree (default file name `file.ts`) the
> rule reports nothing, however Playwright-typed the value is.

**L55**
> The doc's prohibition: a journey stepping around the browser.

**L66**
> Aliasing the type changes the name and not the declaration.


## `.lintcn/no_provider_type_in_signature/no_provider_type_in_signature.go`

**L1** _(kept in source)_
> lintcn:name no-provider-type-in-signature
> lintcn:severity error
> lintcn:description Disallow signatures in the hexagon from naming types a package dependency or the transport owns

**L17**
> The inside, not the whole tree. `**/hexagon/**` also matched
> `hexagon/adapters/**`, where a transport type is the adapter's entire job —
> so this rule condemned a driving adapter for touching a `Response`.

**L84**
> isBranded reports whether a type carries a property keyed by a unique symbol
> this repository declares — the mark a caller cannot forge, because the symbol
> is not exported.
> 
> The key is judged, not the value: the brand property's own type is `true`,
> and it is the computed key that makes the shape unassemblable.
> 
> The declaring file has to be judged too. `string` carries `[Symbol.iterator]`
> from `lib.es2015.iterable.d.ts`, which is a unique-symbol key and would make
> a bare id read as branded — the recorded way this arm stopped enforcing
> anything.

**L119**
> reportable decides whether a written type name resolves to something a
> package dependency declares nominally.
> 
> A type alias is deliberately not reportable: `z.infer<typeof Schema>` is
> declared in `node_modules` and describes a shape this repository owns, and
> flagging it would make every schema-derived command a violation.

**L135**
> notOurs reports whether a declaration belongs to the platform or a package
> rather than this repository, which is what separates the transport `Request`
> from a domain type that happens to share its name.

**L166**
> A finding is one written type name that puts a signature outside the
> hexagon's own vocabulary, and which of the two arms saw it.

**L174**
> Inline literal: lintcn's discovery matches `Name: "..."` in the source to
> bind the CLI name to this rule, so a const would silently desynchronize
> from lintcn:name above.

**L183**
> judge returns the first written name in an annotation that resolves
> to a vendor's nominal type or the platform's transport type.

**L238**
> A destructured parameter has no single name. Its declared
> type is still the signature's, so the vendor question is
> asked the same way; the principal-name question cannot be,
> and asking for the name anyway took the whole run down.


## `.lintcn/no_provider_type_in_signature/no_provider_type_in_signature_test.go`

**L23**
> The platform's transport type, declared by a package rather than lib.dom.

**L28**
> A generic helper is a type alias, the shape it describes is ours.

**L33**
> A vendor that publishes its types inside a namespace, so every reference
> to one is written `namespace.Type` rather than as a bare name.

**L46**
> A port this repository declares, which is what a signature here should say.

**L55**
> Standard-library types are not a vendor's object.

**L60**
> A schema-derived command: declared through an alias in node_modules,
> describing a shape this repository owns.

**L71**
> The doc's own principal: branded with a non-exported unique symbol,
> and narrowed by intersection rather than replaced.

**L86**
> A parameter the vocabulary does not call identity.

**L93**
> A domain command that merely shares the transport's name.

**L101**
> A local `Request` is this repository's own declaration, not the platform's.

**L109**
> A return type this repository owns, wrapped in the language's own Promise.

**L120**
> A schema-derived return: a package alias describing our shape.

**L130**
> A driving adapter holding the transport, which is its whole job.

**L135**
> Destructuring a type this repository owns is ordinary, and naming no
> parameter must not stop the rule reaching the next file.

**L145**
> A namespaced type this repository declares. Asking a qualified name
> for its text as if it were an identifier took the whole run down, so
> every file after it went unjudged.

**L155**
> The gate: outside the inside trees (default file name `file.ts`) the
> rule reports nothing, however vendor-heavy the signature is.

**L165**
> A framework's request object reaching application policy.

**L175**
> A port method's parameter, and the vendor type nested in a generic.

**L187**
> Identity as a bare id: possession of the string is enough.

**L195**
> A principal-shaped object anyone can write out.

**L204**
> A parameter named identity only by configuration.

**L215**
> A port method answering in the driver's vocabulary, nested in a Promise.

**L228**
> A vendor object held as a member of an inside contract.

**L238**
> Renamed on import; the checker follows the alias.

**L248**
> A function-typed member returning the vendor's object reports once.

**L260**
> The platform's Request crossing into the inside.

**L270**
> Headers as a port method's answer.

**L282**
> The standard library's own transport type, pulled in by reference.

**L291**
> A transport type named only by configuration.

**L302**
> The transport renamed on import: a new name, the same declaration.

**L313**
> A destructured parameter has no name to blame, and the rule used to
> ask for one anyway and take the whole run down with it. The type is
> still the signature's, so the vendor question is still asked.

**L325**
> A vendor's namespaced type, reported by the name it is declared under
> rather than the namespace it is reached through.

**L336**
> The same, nested one namespace deeper.


## `.lintcn/port_behaviour_is_an_interface/port_behaviour_is_an_interface.go`

**L1** _(kept in source)_
> lintcn:name port-behaviour-is-an-interface
> lintcn:severity error
> lintcn:description Disallow declaring a port's behaviour as a type alias instead of an interface

**L38**
> readableAsWritten reports whether `port-contract-is-an-interface` already
> judges this declaration. That rule reads a written `TSFunctionType` on an
> exported alias, so a callable spelled out in place is its diagnostic, not
> ours — both firing made one mistake produce two sentences saying the same
> thing. Everything it cannot see is still this rule's: an alias, an
> intersection, and any declaration it never opens.

**L70**
> Inline literal: lintcn's discovery matches `Name: "..."` in the source to
> bind the CLI name to this rule, so a const would silently desynchronize
> from lintcn:name above.

**L100**
> Only a declared object or intersection carries members of its own.
> A primitive's apparent type carries `String.prototype`, whose
> methods are callable and none of this repository's business.

**L107**
> One report per alias: the first callable member is enough to
> settle which keyword the declaration should have used.


## `.lintcn/port_behaviour_is_an_interface/port_behaviour_is_an_interface_test.go`

**L15**
> Outside a port tree the same declaration is nobody's business: this
> rule reported on the repository's own `packages/oxlint/rule.ts` until it
> learnt to read its own path.

**L22**
> The gate: outside the ports tree (default file name `file.ts`) the
> rule reports nothing, however behaviour-shaped the alias is.

**L27**
> A data shape, which is what `type` is reserved for here.

**L29**
> Behaviour already declared with the keyword this rule asks for.

**L35**
> Data reached through an alias is still data.

**L40**
> A union of literals has no members to be callable.

**L42**
> Not exported, so not a published contract.

**L44**
> Written as a function type on an exported alias, which is exactly what
> `port-contract-is-an-interface` reads. Both rules reporting made one
> mistake produce two diagnostics saying nearly the same sentence.

**L48**
> A type literal whose member is written as a function type: the syntactic
> rule reports this one, so this rule does not.

**L57**
> The recorded gap: the member's callability is behind an alias, so the
> syntactic rule reads it as data.

**L67**
> The alias itself is behind an alias.

**L76**
> An intersection carrying behaviour is still a behaviour contract.

**L85**
> Configured to judge unexported aliases too, which the syntactic rule
> never opens — so there is no second diagnostic to collide with.


## `.lintcn/published_contract_publishes_no_mutable_value/published_contract_publishes_no_mutable_value.go`

**L1** _(kept in source)_
> lintcn:name published-contract-publishes-no-mutable-value
> lintcn:severity error
> lintcn:description Disallow a published contract from exposing a value any context can mutate

**L17**
> The writers a standard-library container exposes. Only members the language
> itself declares are judged against this list — a published operation of this
> repository's own named `set` is a command, not a mutation of shared state.

**L55**
> languageDeclared reports whether a member comes from the standard library,
> which is what separates a container's `set` from a contract's own operation.

**L71**
> Inline literal: lintcn's discovery matches `Name: "..."` in the source to
> bind the CLI name to this rule, so a const would silently desynchronize
> from lintcn:name above.

**L97**
> A published factory is behaviour, not shared state. What it
> returns is the caller's own value, not one every context reads.

**L105**
> The container arm runs first. An `Array` is caught by both —
> its `length` is writable and its `push` is a writer — and
> "publish the readonly view" is the repair that fits it.

**L121**
> A method is behaviour. Judging it as state reads every
> interface with a method as mutable, `ReadonlyMap`
> included, which is how this check stops meaning anything.

**L138**
> A `const` carries its `export` on the statement, two nodes up.


## `.lintcn/published_contract_publishes_no_mutable_value/published_contract_publishes_no_mutable_value_test.go`

**L15**
> The blind spot the syntactic rule records: `Object.freeze({…})` and
> `buildRegistry()` are the same syntax, and opposite contracts.

**L21**
> The blind spot the syntactic rule cannot close: a built value, whose
> immutability is only visible once the call's return type resolves.

**L31**
> Readonly containers, which publish nothing a consumer can change.

**L36**
> `new Map()` is the same syntax as a mutable one; only the annotation
> says which this is.

**L42**
> A published operation is behaviour, not shared state.

**L47**
> This contract's own `set` is a command, not a container's writer.

**L56**
> Not exported.

**L64**
> The gate: outside the public tree (default file name `file.ts`) the
> rule reports nothing, however mutable the published value is.

**L70**
> The blind spot, the other way round: same syntax as the frozen one.

**L79**
> The same built value, one `readonly` short.

**L89**
> A published array a consumer can push to.

**L95**
> Shared mutable state behind a published name.


## `.lintcn/read_port_returns_an_answer/read_port_returns_an_answer.go`

**L1** _(kept in source)_
> lintcn:name read-port-returns-an-answer
> lintcn:severity error
> lintcn:description Disallow read port methods that return nothing instead of the answer

**L18**
> The same vocabulary `arch/read-port-writes-nothing` uses to decide which
> declarations are read ports. That rule judges member names; this one judges
> what the member gives back.

**L47**
> Inline literal: lintcn's discovery matches `Name: "..."` in the source to
> bind the CLI name to this rule, so a const would silently desynchronize
> from lintcn:name above.

**L81**
> `Promise<void>` is the shape this rule exists for: the
> annotation reads as a value and resolves to nothing.


## `.lintcn/read_port_returns_an_answer/read_port_returns_an_answer_test.go`

**L15**
> A read that answers with rows.

**L25**
> A write port is allowed to answer with nothing.

**L34**
> An answer that may be absent is still an answer.

**L43**
> An unexported local type is no contract another module depends on.

**L52**
> The gate: outside the ports tree (default file name `file.ts`) the
> rule reports nothing, however answerless the read is.

**L62**
> A configured `files` scope replaces the default tree.

**L72**
> The alias the syntactic rule cannot follow: the annotation says
> `Acknowledged`, the type says nothing at all.

**L84**
> A member whose name carries no write verb at all, so the JS rule is
> silent on it.


## `.lintcn/stored_state_switch_has_a_throwing_default/stored_state_switch_has_a_throwing_default.go`

**L1** _(kept in source)_
> lintcn:name stored-state-switch-has-a-throwing-default
> lintcn:severity error
> lintcn:description Require a throwing `default` on driven-adapter switches deciding on a property of a persistence row type — the row type is a claim about the database, not a fact about it

**L16**
> Where a persistence row type is declared. The row is the adapter's own
> vocabulary — `no-row-type-in-domain` is the rule that keeps it there — so a
> discriminant reaching one is a discriminant read out of the database.

**L50**
> discriminantOfARow reports whether a switch is deciding on a property of a
> type a persistence adapter declares, and names the property.
> 
> This is the whole reason the rule is type-aware. `switch (row.state)` and
> `switch (command.state)` are the same syntax; only the resolved type of the
> thing being read says which of them came out of the database.

**L76**
> throws reports whether a clause's statements reach a `throw`, including one
> inside the block a `never`-binding default is usually written as.

**L89**
> A nested function's `throw` is not this clause's.

**L101**
> Inline literal: lintcn's discovery matches `Name: "..."` in the source to
> bind the CLI name to this rule, so a const would silently desynchronize
> from lintcn:name above.


## `.lintcn/stored_state_switch_has_a_throwing_default/stored_state_switch_has_a_throwing_default_test.go`

**L13**
> The row type, declared where `no-row-type-in-domain` keeps it.

**L20**
> A command the model itself declares. Same syntax at the switch, opposite
> provenance: nothing read it out of a database.

**L33**
> aggregate-reconstitution.md's own mapper, `never` binding and all.

**L53**
> A domain command's discriminant is not a stored one, and upstream
> `switch-exhaustiveness-check` is the right rule for it. A throwing
> default here would be the noise this rule is careful not to demand.

**L71**
> Not a property access, so nothing says where the value came from.

**L86**
> Outside the judged tree.

**L102**
> The gate: outside the driven-adapter tree (default file name
> `file.ts`) the rule reports nothing, however stored the discriminant
> is — here the very switch the first invalid case reports.

**L121**
> Exhaustive over the row type, and silent about the database. This is
> exactly what upstream `switch-exhaustiveness-check` is satisfied by.

**L140**
> A `default` that returns instead of throwing turns a corrupt row into
> a value the model accepts.

**L158**
> A `throw` inside a nested function is not this clause's.


## `.lintcn/use_case_result_is_discriminated/use_case_result_is_discriminated.go`

**L1** _(kept in source)_
> lintcn:name use-case-result-is-discriminated
> lintcn:severity error
> lintcn:description Disallow a use-case result a caller cannot narrow on — a union of object shapes with no shared literal discriminant, or a failure member whose type admits any string

**L23**
> `reason` is deliberately not a default: `bounded-context.md` and
> `domain-probe.md` declare `reason: string` in their own Perfect Examples.
> `errors` is not one either: it is RFC 9457's validation-error array, which
> `api-design/resources/problem-details.md:51-54` endorses by example.

**L44**
> The outcome a caller finally receives: a curried use case returns a function,
> and the union this rule judges is behind the last call.

**L54**
> An array of unconstrained strings is the same failure once per element, so
> the elements decide as much as the type itself does. The bound stops a
> self-referential element type from recurring without end.

**L78**
> genericFailureReasons names the members of a result whose resolved type
> admits any `string`, each once, however many shapes carry them.

**L125**
> Nullish members are an absence, not an outcome shape; a `Row | null` return
> is not the shape this rule is about.

**L162**
> Inline literal: lintcn's discovery matches `Name: "..."` in the source to
> bind the CLI name to this rule, so a const would silently desynchronize
> from lintcn:name above.

**L205**
> A `const` carries its `export` on the statement, two nodes up.


## `.lintcn/use_case_result_is_discriminated/use_case_result_is_discriminated_test.go`

**L17**
> A discriminated union: one literal field says which member this is.

**L28**
> The curried use case the docs actually ship.

**L43**
> An absent answer is not an undiscriminated union.

**L51**
> Not exported, so not the use case's published outcome.

**L58**
> aggregate-root.md's own transition: the next state or an explicit
> refusal, discriminated on a boolean literal.

**L71**
> Specific reason literals: the compiler can prove every failure handled.

**L81**
> `reason: string` is left to configuration: two docs declare it themselves.

**L91**
> RFC 9457's validation-error array, which problem-details.md endorses
> by example, so `errors` is deliberately not seeded.

**L100**
> An array whose elements are modelled reasons: the caller can still
> switch, once per element.

**L110**
> An array of identifiers, which are strings because they identify things.

**L118**
> A generic that carries a string without being a collection of them:
> only arrays and tuples have elements this rule looks inside.

**L128**
> The gate: outside both hexagon trees (default file name `file.ts`)
> the rule reports nothing, however undiscriminated the result is.

**L138**
> A union with nothing to switch on — the caller has to guess by probing
> for a field.

**L149**
> A shared key whose type is widened to `string` discriminates nothing,
> and reads identically to the good version in the source.

**L162**
> Named members still need a shared discriminant; these have none in common.

**L173**
> The same claim one tree over: a transition returning the next state
> or a refusal, with nothing to narrow on.

**L186**
> A widening hidden behind an alias, which the written annotation never shows.

**L198**
> No annotation at all: the inferred `error` is whatever `message` was.

**L209**
> A single shape, not a union, still answers with a bare string.

**L218**
> A literal widened by a bare `string` sibling collapses to `string`.

**L229**
> `reason` judged only once configured.

**L241**
> The same claim one tree over.

**L254**
> Both arms on one function: undiscriminated, and a generic error.

**L266**
> An array of unconstrained strings is the same failure once per element,
> and a `readonly string[]` is a reference type rather than a flagged one.

**L276**
> The refusal shape a repository following this tree actually shipped.

**L285**
> The other two spellings the same shape takes.


## `.lintcn/use_case_throws_a_domain_error/use_case_throws_a_domain_error.go`

**L1** _(kept in source)_
> lintcn:name use-case-throws-a-domain-error
> lintcn:severity error
> lintcn:description Disallow throwing error types this repository does not declare inside the hexagon — a package dependency anywhere in it, a standard-library Error in the application tree

**L19**
> A bare `Error` is the hexagonal skill's own way to reject an impossible
> state at construction — its worked example throws one from `createMoney`
> while returning `exceeds-budget` as a result value in the same function. An
> invariant violation is not a business outcome, so the standard-library arm
> judges the application tree only; the package-dependency arm judges both.

**L49**
> Inline literal: lintcn's discovery matches `Name: "..."` in the source to
> bind the CLI name to this rule, so a const would silently desynchronize
> from lintcn:name above.

**L65**
> A rethrown `unknown` has no declaration to judge, and
> guessing is how a rule starts reporting on nothing.


## `.lintcn/use_case_throws_a_domain_error/use_case_throws_a_domain_error_test.go`

**L22**
> An error type the domain declares, which carries the business meaning.

**L30**
> A rethrow has nothing declared to judge.

**L37**
> An adapter translating a vendor failure is doing its job.

**L42**
> A factory rejecting an impossible state. The hexagonal skill's own
> worked example throws a bare `Error` from `createMoney` while
> returning `exceeds-budget` as a result value in the same function:
> an invariant violation is not a business outcome, and the domain has
> no result union to put it in.

**L56**
> The gate: outside both hexagon trees (default file name `file.ts`)
> the rule reports nothing, however foreign the thrown type is.

**L62**
> A bare `Error` for a business outcome: every surface must invent copy.

**L68**
> A vendor's error escaping the inside, which the package-dependency arm
> judges in the domain tree as well.

**L79**
> An alias hides the name, never the declaration.

**L88**
> A repository that wants the stricter reading opts the domain tree in.

**L95**
> A vendor error thrown in the domain still reports: no skill sanctions
> a provider's failure type reaching the model.

